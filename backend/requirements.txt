# ── Web / API ───────────────────────────────────────────────────────────
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# ── Core / config ───────────────────────────────────────────────────────
pydantic>=2.10.0
pydantic-settings>=2.0
python-dotenv>=1.0.0
pyyaml>=6.0            # persona engine configs (app/engine/personas/configs/*.yaml)

# ── Data layer ──────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.19
yfinance>=0.2.32
pandas>=2.1.0
httpx>=0.27.0

# ── Auth ────────────────────────────────────────────────────────────────
supabase>=2.13.0

# ── LLM (optional; the deterministic pipeline runs without any key) ──────
# Default provider is Groq via the `groq` SDK — no LangChain required.
# Other providers use the lazy factory in app/engine/llm.py; add the matching
# langchain-* package (e.g. langchain-openai) only if you use them.
groq>=0.3.0
google-generativeai>=0.3.0

# NOTE: `langgraph` was removed in Phase 3 (see docs/adr/001-remove-langgraph.md).
# NOTE: `apscheduler` (the fund scheduler) arrives in Phase 4.
# NOTE: PyPDF2 / pytesseract / Pillow remain only for the legacy app/main.py
#       document endpoints; they are pruned when main.py is rewritten in Phase 4.
PyPDF2>=3.0.1
pytesseract
Pillow
