# PortAI — Benchmarks

Generated by `scripts/bench.py` (offline mode) on 2026-07-13.

> Offline harness: a deterministic synthetic provider sits **behind the real read-through cache**, with a simulated 10.0 ms per provider call. Cache hit-rate, provider-call reduction and compute latency are measured for real; network is excluded. Regenerate against live data with `python scripts/bench.py --live --write`.

Universe (6): `RELIANCE.NS, TCS.NS, HDFCBANK.NS, INFY.NS, AAPL, MSFT`

| Metric | Cold (empty cache) | Warm (cache hot) |
|---|---:|---:|
| Full-universe analysis latency | 0.227 s | 0.071 s |
| Provider calls per analysis | 33 | 0 |
| Cache hit-rate | — | 100.0% |

- **Warm speedup:** 3.19×
- **Provider-call reduction:** 33 → 0 (daily OHLCV for past dates is immutable and never refetched; singleflight collapses concurrent misses to one call).
- **LLM calls / cost this run:** 0 / $0.000000 (0 without an LLM key — the deterministic pipeline needs none; set `GROQ_API_KEY` and `--memo` to measure memo cost).

## Deltas vs Phase-0 baseline (`docs/BASELINE.md`)

| Metric | Baseline | Now |
|---|---:|---:|
| `backend/app/main.py` | 1463 lines | 49 lines |
| Frontend pages | 57 | 6 surfaces (+ auth) |
| LangGraph dependency | present | removed (ADR-001) |
| Provider calls per warm analysis | every request hit yfinance | 0 |
